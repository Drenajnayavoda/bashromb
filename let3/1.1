
#!/bin/bash

declare -a str

while IFS="" read -r -n 1 x; do
  str+=("$x")
done <<< $1

while IFS="" read -r -n 1 x; do
  str+=("$x")
done <<< $2

readarray -t -d '' str2 < <(printf '%s\0' "${str[@]}" | sort -u -z )

for i in ${str2[@]}; do
  echo -n $i
done

echo

exit 0
